# OwnerDirectory

{% partial file="/docs/_snippets/_single-asset-vault-disclaimer.md" /%}

`Vault` objects are stored in the ledger and tracked in an [Owner Directory]() owned by the account submitting the `VaultSet` transaction. To facilitate `Vault` object lookup from the vault shares, the object is also tracked in the OwnerDirectory of the pseudo-account.
