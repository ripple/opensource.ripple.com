# Confidential Transfers (Python)

Demonstrates how to use confidential transfers on the XRP Ledger.

Quick setup and usage:

```sh
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python confidential-transfers.py
```
